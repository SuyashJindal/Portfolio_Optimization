# Portfolio_Optimization
Also download 
# 1. Install dependencies
pip install -r requirements.txt
In this :
python run_app.py
will load browser of streamlit.
Deploy by running locally on server using  directly run  python file ->run_app.py
#Docker Based Deployment 
Download zip file and extract it .
In terminal:-
# Command run -> docker compose up --build
Docker App be there for it.
# Also deploy on streamlit
